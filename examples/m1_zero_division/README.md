# M1 disposable demo

`calculator.py` intentionally contains a zero-division bug. The test suite defines the expected behavior. Use `mini-code demo-m1` or `start.cmd demo`; the launcher copies this template before allowing the Agent to edit it.
